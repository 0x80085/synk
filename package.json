{
  "name": "synk",
  "version": "0.4.4",
  "description": "Back and Front end for SYNK",
  "main": "index.js",
  "dependencies": {
    "nodemon": "^2.0.16"
  },
  "devDependencies": {
    "concurrently": "^7.1.0"
  },
  "scripts": {
    "start": "npx concurrently \"cd synk-client && ng s --live-reload\" \"cd synk-api && npm start\"",
    "docker:run": "docker-compose -f \"docker-compose.yml\" up -d --build"
  },
  "author": "0x80085",
  "license": "MIT"
}
